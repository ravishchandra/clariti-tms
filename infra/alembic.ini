# Alembic configuration — clariti-tms
# Run from the repo root: alembic --config infra/alembic.ini <command>

[alembic]
# Script location is relative to this file's directory
script_location = %(here)s/migrations

# sqlalchemy.url is intentionally blank — env.py reads it from Settings.
sqlalchemy.url =

# Logging
[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
