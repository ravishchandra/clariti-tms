# SDK Generation

1. Start the dev server: `make dev`
2. Fetch the live spec: `make spec`  
3. Generate both SDKs: `make sdks`

SDKs land in sdks/typescript/ and sdks/python/.
Publish TypeScript as @clariti-tms/sdk (npm).
Publish Python as clariti-tms (PyPI).
