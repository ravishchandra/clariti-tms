# Changelog

## 0.1.0

Initial release. `ClaritiTMSOTA.fetchLocale` — fetch published translations for
a project + locale with ETag caching (`shared_preferences`) and a bundled-file
fallback on 404 / 5xx / network / parse error. Flutter counterpart to the iOS
and Android OTA SDK sketches in `docs/12-ota.md`.
